extends layouts/main-layout

block styles 
    link(rel="stylesheet", href="/css/user.css")

block content 
    h1 Users
    
    if users.length > 0
        ul 
            each user in users 
                li #{user}
    else 
        p No users found