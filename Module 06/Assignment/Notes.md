1. Create a npm project and install expres.js (Nodemon if you want)
1. Add two routes:
    * "/" => Holds a ```<form>``` that allows users to input their name
    * "/users" => Outputs an ```<ul>``` with the user names (or some error text)